export default defineAppConfig({
  ui: {
    colors: {
      primary: "green",
      neutral: "zinc",
    },
    container: {
      base: "w-full max-w-(--ui-container) mx-auto px-2 sm:px-6 lg:px-8 py-4 lg:py-8",
    },
  },
});
